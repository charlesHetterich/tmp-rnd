{
  "name": "my-contract-startup",
  "version": "0.1.0",
  "main": "src/index.ts",
  "scripts": {
    "build": "hardhat clean && hardhat compile",
    "test": "hardhat test"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^2.0.2",
    "@parity/hardhat-polkadot": "^0.1.8",
    "hardhat": "^2.24.0",
    "hardhat-contract-sizer": "^2.10.0",
    "hardhat-storage-layout": "^0.1.7"
  },
  "files": [
    "hardhat.config.js"
  ]
}
