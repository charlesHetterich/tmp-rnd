{
  "name": "polkavm-contracts",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "papi": "papi",
    "generate": "papi generate"
  },
  "devDependencies": {
    "polkadot-api": "^1.9.5"
  },
  "dependencies": {
    "@polkadot-api/descriptors": "file:.papi/descriptors"
  }
}
